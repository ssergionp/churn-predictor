DATABASE_URL=postgresql+psycopg://churn:churn@localhost:5433/churn_db

# Comma-separated list of origins allowed by CORS (e.g. your deployed
# frontend's URL in production). Defaults to the local Vite dev server.
ALLOWED_ORIGINS=http://localhost:5173
